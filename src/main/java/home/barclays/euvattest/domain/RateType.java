package home.barclays.euvattest.domain;

/**
 * A type of rates: it can be standard, reduced, super reduced, etc. There is only a standard type for shortness here
 * */
public enum RateType {
    STANDARD
}